/**
 * The web has no phone to buzz (PLAN.md 12.19): see `index.web.ts`. The trail and the Activity screen say what traded.
 */
import type { RegistrationResult } from './index';

export function useTradeNotifications(_push: RegistrationResult | undefined): void {}

# Features that would help xorr win

100 ideas not in the repo today (each grep-checked 2026-09-14), scored impact × feasibility × fit on 1–5 scales, highest first.

| # | Idea | What it is | Bucket | Impact | Feasibility | Fit | Score | Main files |
|---|---|---|---|---|---|---|---|---|
| 1 | Stop works with our server down | Safety reads the permission from chain and signs revoke even when the executor is down. | Core | 5 | 5 | 5 | 125 | `app/safety.tsx`, `src/auth/useGrantDelegation.web.ts`, `src/auth/useGrantDelegation.native.ts`, `src/wallet/chainAccess.ts` |
| 2 | Watch the chain refuse the bot | Dry-run over-cap, wrong-venue and post-revoke spends; show the contract's own reverts. | Core | 5 | 4 | 5 | 100 | `server/src/routes/index.ts`, `server/src/evm/delegation.ts`, `server/src/executor/failure.ts`, `app/safety.tsx` |
| 3 | Hold-to-stop kill switch | The stop button fills while held and commits at 600 ms, with one haptic. | Design | 4 | 5 | 5 | 100 | `src/ui/Button.tsx`, `src/ui/motion.ts`, `app/safety.tsx` |
| 4 | Cap dial showing the worst case | A cap marker slides along a risk gradient beside the most it could ever spend. | Design | 4 | 5 | 5 | 100 | `app/(onboarding)/delegate.tsx`, `src/ui/charts/Ruler.tsx`, `src/state/derived.ts` |
| 5 | Portfolio graph from recorded snapshots | Draw Portfolio from stored snapshots with 1D, 1W, 1M and All ranges. | Core | 4 | 5 | 5 | 100 | `app/portfolio.tsx`, `src/data/system.ts`, `src/ui/charts/AreaChart.tsx` |
| 6 | Checks that land as they finish | Stream /verify so each check lands on screen the moment it finishes. | Design | 4 | 4 | 5 | 80 | `server/src/routes/verify.ts`, `server/src/verify/checks.ts`, `app/judge.tsx`, `src/data/api.ts` |
| 7 | Stop receipt with measured timings | After a stop, show when the chain, the index and the bot each saw it. | Core | 4 | 4 | 5 | 80 | `app/safety.tsx`, `server/src/routes/index.ts`, `server/src/graph/client.ts`, `server/src/rules/engine.ts` |
| 8 | Money path on How it works | Draw your last real fill's path: wallet, permission, venue, back to your wallet. | Design | 4 | 4 | 5 | 80 | `app/sponsors.tsx`, `src/ui/charts/MoneyPath.tsx` (new), `server/src/routes/extra.ts` |
| 9 | Your fills marked on charts | Buy and sell marks sit on the exact candles where the bot filled. | Design | 4 | 4 | 5 | 80 | `src/ui/charts/Candlestick.tsx`, `src/ui/charts/AreaChart.tsx`, `app/asset/[symbol].tsx`, `app/position/[id].tsx` |
| 10 | Best-execution receipt on every fill | Store every venue's answer at fill time; each run shows what the runner-up offered. | Sponsor | 4 | 4 | 5 | 80 | `server/src/executor/settle.ts`, `server/src/executor/run.ts`, `server/src/db/migrations/` (new), `app/runs/[id].tsx` |
| 11 | Limit orders compete in routing | Open 1inch limit orders join Aqua, SwapVM and the aggregator in best execution. | Sponsor | 4 | 4 | 5 | 80 | `server/src/executor/settle.ts`, `server/src/venues/limit-orders.ts`, `server/src/venues/compare.ts` |
| 12 | Verify the trail on your device | Re-hash the exported trail on the device and compare it to the on-chain anchor. | Core | 4 | 4 | 5 | 80 | `app/audit/anchor.tsx`, `src/wallet/chainAccess.ts`, `src/data/system.ts`, `server/src/audit/log.ts` |
| 13 | Index flags spends we didn't make | Fail a check and alert when the subgraph shows a spend no executor run made. | Sponsor | 4 | 4 | 5 | 80 | `server/src/verify/checks.ts`, `server/src/graph/client.ts`, `server/src/alerts/evaluate.ts` |
| 14 | Setup progress for new wallets | Three steps until the bot has actually traded — fund, permit, first trade — each from the thing that decides it: the real balance, the CHAIN's own account of the permission, and a fill the executor recorded. Four states, not two: a read still out says it is checking, and a read that failed says it could not check. Neither is ever drawn as "not done". | Production | 4 | 5 | 4 | 80 | `app/(tabs)/index.tsx`, `src/state/derived.ts` |
| 15 | Profit and loss per strategy | Each strategy shows what it spent, still holds and realised, from its own runs. | Core | 4 | 4 | 5 | 80 | `server/src/routes/strategies.ts`, `server/src/positions/index.ts`, `app/strategy/[id].tsx` |
| 16 | Owner-signed withdrawal allowlist | Your Privy wallet EIP-712 signs each allowlist entry, so our server cannot add one. | Sponsor | 4 | 4 | 5 | 80 | `app/allowlist.tsx`, `src/wallet/allowlist.ts`, `server/src/withdrawals/allowlist.ts` |
| 17 | Every number shows its source | Long-press any figure to see its source, block or feed, and the call to repeat. | Core | 5 | 3 | 5 | 75 | `src/ui/Text.tsx`, `src/ui/SheetCard.tsx`, `src/data/types.ts`, `server/src/routes/index.ts` |
| 18 | Guardian signer that can only revoke | A Privy session signer whose policy allows only revoke(), fired on a drawdown you set. | Sponsor | 5 | 3 | 5 | 75 | `src/auth/useGuardian.ts` (new), `server/src/auth/privyPolicy.ts`, `server/src/alerts/evaluate.ts`, `app/safety.tsx` |
| 19 | Bot key held by Privy | Move the delegate key into a policy-bound Privy wallet; no key file on our host. | Sponsor | 5 | 3 | 5 | 75 | `server/src/evm/client.ts`, `server/src/auth/privyPolicy.ts`, `server/src/executor/swap.ts`, `server/src/verify/checks.ts` |
| 20 | Graph Node indexing the fork | Run graph-node against the fork so both subgraphs are live where fills settle. | Sponsor | 5 | 3 | 5 | 75 | `infra/graph-node/` (new), `subgraph/subgraph.yaml`, `subgraph-aqua/subgraph.yaml`, `server/src/graph/aqua.ts` |
| 21 | Money-in moment on Deposit | When funds land, the balance rolls to its true value with one success haptic. | Design | 3 | 5 | 5 | 75 | `app/deposit.tsx`, `src/ui/RollingNumber.tsx` |
| 22 | Shareable proof link | Open /judge?owner=0x… straight to that wallet's live checks, and share it. | Core | 3 | 5 | 5 | 75 | `app/judge.tsx`, `app/verify.tsx`, `src/export/deliver.ts` |
| 23 | A small haptic vocabulary | Ticks on selection, success on fills, warning on refusals, a heavy tap on stop. | Design | 3 | 5 | 5 | 75 | `src/ui/Press.tsx`, `src/ui/Switch.tsx`, `src/ui/Segmented.tsx`, `app/safety.tsx` |
| 24 | Pin the contract in the build | Refuse to approve or grant if the executor names another contract address or bytecode. | Production | 3 | 5 | 5 | 75 | `src/chain.ts`, `src/auth/useGrantDelegation.web.ts`, `src/auth/useGrantDelegation.native.ts`, `scripts/build-web.mjs` |
| 25 | Alarm when anchoring stops | Fail verification and alert when the hourly anchor has not published for two hours. | Production | 3 | 5 | 5 | 75 | `server/src/verify/checks.ts`, `server/src/audit/anchor-sweep.ts`, `server/src/alerts/evaluate.ts` |
| 26 | Failed reads never look empty | Inbox and Alerts show a retryable error when a read fails, never an empty list. | Production | 3 | 5 | 5 | 75 | `app/inbox.tsx`, `app/alerts.tsx`, `src/ui/States.tsx` |
| 27 | Fresh numbers on return | Home and Portfolio re-read when revisited or when the app returns to foreground. | Production | 3 | 5 | 5 | 75 | `src/data/useAsync.ts`, `app/(tabs)/index.tsx`, `app/portfolio.tsx` |
| 28 | No fixture roster on failure | When agents cannot load, say so instead of rendering the fixture roster. | Production | 3 | 5 | 5 | 75 | `src/data/local.ts`, `app/(tabs)/index.tsx`, `app/bot/roster.tsx` |
| 29 | Retry-safe money actions | Orders, swaps, closes and faucet claims send Idempotency-Key, so retries never double-fill. | Production | 3 | 5 | 5 | 75 | `src/data/api.ts`, `src/data/repositories.ts`, `app/order/[symbol].tsx`, `app/swap.tsx` |
| 30 | The permission seals on grant | When the grant lands, the four limits settle into one sealed card with its block. | Design | 4 | 4 | 4 | 64 | `app/(onboarding)/delegate.tsx`, `src/ui/SheetCard.tsx`, `src/ui/Rise.tsx` |
| 31 | Agents visibly sleep on stop | When revoke confirms, agent orbs desaturate in turn and Live fades to grey. | Design | 4 | 4 | 4 | 64 | `src/ui/AgentOrb.tsx`, `app/(tabs)/index.tsx`, `app/safety.tsx` |
| 32 | Slim toast when the bot acts | A slim pill reports each fill or refusal the moment it happens. | Design | 4 | 4 | 4 | 64 | `src/ui/Screen.tsx`, `src/data/usePoll.ts`, `server/src/routes/catchup.ts` |
| 33 | Decoded preview before every signature | Before each wallet prompt, show the decoded call and its simulated balance changes. | Core | 4 | 4 | 4 | 64 | `src/wallet/userSigning.ts`, `src/ui/SheetCard.tsx`, `app/(onboarding)/delegate.tsx`, `app/send.tsx` |
| 34 | Cap and term rings on Safety | Two thin rings on Safety: today's cap used, and the permission's time left. | Design | 4 | 4 | 4 | 64 | `app/safety.tsx`, `src/ui/charts/Ring.tsx` (new), `src/data/system.ts` |
| 35 | SwapVM programs decoded into rules | Decode a shipped program's bytecode into its deadline, fee and floor on How it works. | Sponsor | 4 | 4 | 4 | 64 | `server/src/venues/swapvm.ts`, `server/src/routes/extra.ts`, `app/sponsors.tsx` |
| 36 | One maker balance, two venues | One maker's balance backs an Aqua book and a SwapVM program, shown filling both. | Sponsor | 4 | 4 | 4 | 64 | `server/src/fork/makers.ts`, `server/src/fork/ship-makers.ts`, `app/sponsors.tsx` |
| 37 | One-signature grant, gas sponsored | EIP-7702 batches every approval and the grant into one Privy signature; the executor pays gas. | Sponsor | 5 | 3 | 4 | 60 | `contracts/src/XorrBatch.sol` (new), `src/auth/useGrantDelegation.web.ts`, `src/auth/useGrantDelegation.native.ts`, `server/src/routes/index.ts` |
| 38 | Look around as the demo wallet | Signed-out visitors browse the live demo wallet read-only, without an email code. | Core | 5 | 3 | 4 | 60 | `server/src/auth/middleware.ts`, `server/src/auth/agentKeys.ts`, `src/data/api.ts`, `app/(tabs)/index.tsx` |
| 39 | Quote idle cash as a maker | Ship an Aqua book from your own wallet; takers fill it and funds never leave. | Sponsor | 5 | 3 | 4 | 60 | `app/maker.tsx` (new), `server/src/fork/makers.ts`, `server/src/venues/aqua.ts`, `src/wallet/userSigning.ts` |
| 40 | Business treasury with two approvers | A key-quorum-owned Privy wallet grants the bot; raising its cap needs two approvers. | Sponsor | 5 | 3 | 4 | 60 | `app/business.tsx` (new), `server/src/auth/privyPolicy.ts`, `server/src/routes/privy.ts`, `src/data/system.ts` |
| 41 | Dry-run a strategy right now | Ask a strategy what it would do right now, priced and routed, without trading. | Core | 3 | 4 | 5 | 60 | `server/src/routes/strategies.ts`, `server/src/executor/run.ts`, `app/strategy/[id].tsx` |
| 42 | Stop from the home screen | Long-press the app icon, or the installed web app, to open a ready stop. | Core | 4 | 3 | 5 | 60 | `app.json`, `package.json`, `scripts/build-web.mjs`, `app/safety.tsx` |
| 43 | Cut the 11 MB first load | Split routes and Privy's UI out of the 11 MB entry bundle judges download first. | Production | 4 | 3 | 5 | 60 | `app.json`, `metro.config.js`, `scripts/build-web.mjs`, `src/auth/PrivyProvider.web.tsx` |
| 44 | Buttons that narrate confirmation | Confirming buttons count seconds, then morph into a check before showing the new state. | Design | 3 | 4 | 5 | 60 | `src/ui/Button.tsx`, `src/wallet/userSigning.ts` |
| 45 | Scrub line charts with a crosshair | Drag across a line chart for a crosshair with the exact time and value. | Design | 3 | 4 | 5 | 60 | `src/ui/charts/AreaChart.tsx`, `src/ui/charts/Sparkline.tsx`, `app/asset/[symbol].tsx`, `app/portfolio.tsx` |
| 46 | Test send to a new address | Offer a one-dollar test transfer to a new address before the full withdrawal. | Core | 3 | 5 | 4 | 60 | `app/send.tsx`, `app/withdraw-everything.tsx`, `src/wallet/useWithdraw.ts` |
| 47 | Tap to hide balances | Tap the balance to mask every amount app-wide, remembered across launches. | Design | 3 | 5 | 4 | 60 | `app/(tabs)/index.tsx`, `src/state/store.ts`, `src/ui/Text.tsx` |
| 48 | Basenames as withdrawal destinations | Add an allowlist address by typing name.base.eth, resolved and confirmed both ways. | Sponsor | 3 | 5 | 4 | 60 | `app/allowlist.tsx`, `server/src/evm/basename.ts`, `server/src/routes/withdrawals.ts` |
| 49 | One-tap fix on blocked runs | A blocked run offers the one action that clears it: raise cap, renew, fund. | Core | 3 | 5 | 4 | 60 | `app/runs/[id].tsx`, `src/state/derived.ts` |
| 50 | Tomorrow's cap, already committed | Show how much of tomorrow's cap your live strategies already commit. | Core | 3 | 4 | 5 | 60 | `app/limits.tsx`, `server/src/routes/strategies.ts`, `src/data/system.ts` |
| 51 | Anchor history indexed by The Graph | Index XorrAuditAnchor in the subgraph; verification compares trail, chain and index. | Sponsor | 3 | 4 | 5 | 60 | `subgraph/subgraph.yaml`, `subgraph/schema.graphql`, `subgraph/src/mapping.ts`, `server/src/verify/checks.ts` |
| 52 | Stale-index guard in routing | decide() stands aside when the index lags the chain head too far, and says so. | Sponsor | 3 | 5 | 4 | 60 | `server/src/graph/decide.ts`, `server/src/graph/client.ts`, `server/src/executor/run.ts` |
| 53 | App and executor versions compared | Settings shows the app and executor commits and flags when they differ. | Production | 3 | 5 | 4 | 60 | `app/settings.tsx`, `scripts/build-web.mjs`, `src/data/health.ts` |
| 54 | Hired agents breathe | Hired agents' orbs breathe slowly; others stay still, so state reads at a glance. | Design | 2 | 5 | 5 | 50 | `src/ui/AgentOrb.tsx`, `app/(tabs)/index.tsx` |
| 55 | Vault wallet outside the permission | A second Privy wallet the permission never names, for money the bot must not reach. | Sponsor | 4 | 3 | 4 | 48 | `src/auth/embeddedWallet.ts`, `src/auth/useAuth.web.ts`, `server/src/auth/walletBinding.ts`, `app/send.tsx` |
| 56 | Switch off one venue | Remove a single venue from your permission with one signature, no re-grant. | Core | 3 | 4 | 4 | 48 | `app/venues.tsx`, `src/auth/useGrantDelegation.web.ts`, `src/auth/useGrantDelegation.native.ts`, `server/src/routes/index.ts` |
| 57 | Permission history as a timeline | Index each grant, venue change and revoke as history, drawn as one timeline. | Sponsor | 3 | 4 | 4 | 48 | `subgraph/schema.graphql`, `subgraph/src/mapping.ts`, `server/src/routes/extra.ts`, `app/delegation.tsx` |
| 58 | Replay any day of decisions | Scrub a past day: the price, each decision the bot made, and why. | Core | 4 | 3 | 4 | 48 | `app/replay.tsx` (new), `server/src/routes/history.ts`, `server/src/audit/log.ts`, `src/ui/charts/AreaChart.tsx` |
| 59 | Away mode until a date | Pause every strategy until a chosen date; they resume on their own. | Core | 3 | 4 | 4 | 48 | `server/src/rules/engine.ts`, `server/src/executor/scheduler.ts`, `server/src/db/migrations/` (new), `app/safety.tsx` |
| 60 | Web push for refusals and fills | Refusals and fills reach the web app through standard Web Push, no store account. | Core | 4 | 3 | 4 | 48 | `src/notifications/index.web.ts`, `server/src/notifications/push.ts`, `scripts/build-web.mjs` |
| 61 | Buy-the-dip sizing on recurring buys | A recurring buy sizes up when price sits below its 30-day average. | Core | 3 | 4 | 4 | 48 | `server/src/executor/kinds/index.ts`, `src/strategies/ladder.ts`, `app/strategy/dca.tsx`, `server/src/backtest/engine.ts` |
| 62 | Choose the weekday and hour | Schedule a recurring buy for a weekday and hour, not just a cadence. | Core | 3 | 4 | 4 | 48 | `src/strategies/schedule.ts`, `server/src/executor/schedule.ts`, `app/strategy/dca.tsx` |
| 63 | Recurring buy versus one buy | Compare a running recurring buy against buying it all on day one. | Core | 3 | 4 | 4 | 48 | `app/strategy/[id].tsx`, `server/src/routes/strategies.ts`, `server/src/market/prices.ts` |
| 64 | First strategy during onboarding | Onboarding offers one real recurring buy beside the suggested sleeves. | Core | 3 | 4 | 4 | 48 | `app/(onboarding)/proposal.tsx`, `app/(onboarding)/goals.tsx`, `app/strategy/dca.tsx` |
| 65 | Drift from target weights | Show how far each sleeve sits from its approved weight, and when rebalance acts. | Core | 3 | 4 | 4 | 48 | `app/(tabs)/holdings.tsx`, `src/state/derived.ts`, `server/src/routes/strategies.ts` |
| 66 | Installable web app | A manifest, icons and offline shell so the web app installs like the phone app. | Production | 3 | 4 | 4 | 48 | `scripts/build-web.mjs`, `app.json`, `assets/` |
| 67 | Stale fork warning before trades | Warn before trading when the fork has drifted too far behind live Base. | Production | 3 | 4 | 4 | 48 | `server/src/routes/ops.ts`, `server/src/evm/measure-route.ts`, `infra/base-fork/entrypoint.sh`, `app/order/[symbol].tsx` |
| 68 | Last known values, dated, offline | Offline, balance and positions show their last real read with its time. | Production | 3 | 4 | 4 | 48 | `src/state/store.ts`, `src/data/useAsync.ts`, `app/(tabs)/index.tsx` |
| 69 | Nightly live smoke on hosted | A scheduled job grants, fills and stops on the hosted fork and publishes the result. | Production | 3 | 4 | 4 | 48 | `.github/workflows/ci.yml`, `server/src/live-agents.ts`, `tools/demo.mjs` |
| 70 | Hairline highlights on raised cards | Raised cards get a one-pixel top highlight so layers read without borders. | Design | 2 | 5 | 4 | 40 | `src/ui/tokens.ts`, `src/ui/SheetCard.tsx` |
| 71 | Tab selection glides | Tab and sheet-tab underlines glide to the new selection in 180 ms. | Design | 2 | 5 | 4 | 40 | `src/ui/TabBar.tsx`, `app/(tabs)/index.tsx` |
| 72 | Swap flip that turns | The flip circle turns half a revolution as the pair trades places. | Design | 2 | 5 | 4 | 40 | `app/swap.tsx` |
| 73 | Screen readers hear money moments | Screen readers announce fills, stops and refusals as they happen. | Production | 2 | 5 | 4 | 40 | `src/ui/announce.ts` (new), `app/safety.tsx`, `app/order/[symbol].tsx`, `app/swap.tsx` |
| 74 | Headings and spoken chart summaries | Screen titles become headers; every chart carries a spoken summary of its range. | Production | 2 | 5 | 4 | 40 | `src/ui/Text.tsx`, `src/ui/IconButton.tsx`, `src/ui/charts/AreaChart.tsx`, `src/ui/charts/Candlestick.tsx` |
| 75 | Fallback RPCs for every read | Reads and broadcasts fail over to a second RPC instead of erroring. | Production | 2 | 5 | 4 | 40 | `src/wallet/chainAccess.ts`, `src/chain.ts`, `server/src/evm/client.ts` |
| 76 | Security headers on the web app | Ship CSP, frame-ancestors, referrer and permissions policies in the generated vercel.json. | Production | 2 | 5 | 4 | 40 | `scripts/build-web.mjs` |
| 77 | Verification results attested on-chain | Publish each verify summary as an EAS attestation anyone can read on easscan. | Sponsor | 3 | 4 | 3 | 36 | `server/src/evm/eas.ts` (new), `server/src/verify/checks.ts`, `server/src/audit/anchor-sweep.ts`, `app/judge.tsx` |
| 78 | Slice large orders over time | Split a large buy into timed slices, each settled at best execution. | Core | 3 | 3 | 4 | 36 | `server/src/executor/kinds/index.ts`, `server/src/executor/order.ts`, `app/order/[symbol].tsx` |
| 79 | Speed up a stuck transaction | A user transaction pending too long offers a same-nonce replacement with a higher fee. | Production | 3 | 3 | 4 | 36 | `src/wallet/userSigning.ts`, `app/send.tsx`, `app/safety.tsx` |
| 80 | Signed webhooks for every decision | Post fills, refusals and stops to a business endpoint, HMAC-signed and retried. | Core | 3 | 4 | 3 | 36 | `server/src/notifications/webhooks.ts` (new), `server/src/audit/log.ts`, `app/notifications.tsx` |
| 81 | Scoped API keys in-app | Mint, scope and revoke agent API keys in-app for a business's own automation. | Core | 3 | 4 | 3 | 36 | `app/keys.tsx` (new), `server/src/routes/agent.ts`, `server/src/auth/agentKeys.ts`, `app/explore.tsx` |
| 82 | Titles collapse on scroll | Large screen titles shrink into the top bar on scroll, like native large titles. | Design | 2 | 4 | 4 | 32 | `src/ui/IconButton.tsx`, `src/ui/Screen.tsx` |
| 83 | Range switches without jumps | Switching 1D to 1W crossfades the line while the axis holds its place. | Design | 2 | 4 | 4 | 32 | `src/ui/charts/AreaChart.tsx`, `app/asset/[symbol].tsx` |
| 84 | Flashblocks preconfirmed receipts | Read Flashblocks preconfirmations so grants and stops confirm in about 200 ms. | Sponsor | 2 | 4 | 4 | 32 | `src/chain.ts`, `src/wallet/chainAccess.ts`, `server/src/evm/client.ts` |
| 85 | Virtualized long lists | Activity, History, Runs and Inbox render with FlashList, smooth past thousands of rows. | Production | 2 | 4 | 4 | 32 | `app/activity.tsx`, `app/history.tsx`, `app/runs/index.tsx`, `app/inbox.tsx` |
| 86 | Large text without broken rows | Honour large text up to a per-variant cap so rows wrap, never clip. | Production | 2 | 4 | 4 | 32 | `src/ui/Text.tsx`, `src/ui/type.ts`, `src/ui/Row.tsx` |
| 87 | Sessions that lapse resume the action | If a session lapses mid-action, signing back in returns to the same step. | Production | 2 | 4 | 4 | 32 | `src/data/apiError.ts`, `src/auth/useAuth.web.ts`, `src/nav/useGoBack.ts` |
| 88 | Locale-safe amount entry | Typed amounts accept comma decimals and pasted values, refusing anything ambiguous. | Production | 2 | 4 | 4 | 32 | `src/wallet/transfer.ts`, `app/swap.tsx`, `app/send.tsx` |
| 89 | Nonce manager for the delegate | Serialize delegate nonces so strategies firing together never collide or stall. | Production | 2 | 4 | 4 | 32 | `server/src/evm/client.ts`, `server/src/executor/scheduler.ts` |
| 90 | Request id on every error | Errors carry the executor's request id, copyable, so a report points at logs. | Production | 2 | 5 | 3 | 30 | `src/data/api.ts`, `src/data/apiError.ts`, `src/ui/States.tsx` |
| 91 | Book depth under the order ticket | The order ticket draws book depth and marks where this size lands. | Sponsor | 3 | 3 | 3 | 27 | `app/order/[symbol].tsx`, `server/src/venues/aqua.ts`, `server/src/routes/market.ts` |
| 92 | Per-asset caps enforced on chain | Add a per-token daily cap to XorrDelegation, enforced on chain beside the total. | Core | 4 | 2 | 3 | 24 | `contracts/src/XorrDelegation.sol`, `contracts/test/XorrDelegation.t.sol`, `subgraph/src/mapping.ts`, `app/(onboarding)/delegate.tsx` |
| 93 | Balance carries into Portfolio | The Home balance moves into Portfolio's header instead of the screen cutting. | Design | 3 | 2 | 4 | 24 | `app/(tabs)/index.tsx`, `app/portfolio.tsx` |
| 94 | Privy MFA to widen permission | Raising the cap or resuming asks for Privy MFA, not only device biometrics. | Sponsor | 3 | 2 | 4 | 24 | `app/(onboarding)/delegate.tsx`, `app/safety.tsx`, `src/auth/PrivyProvider.web.tsx`, `src/auth/PrivyProvider.native.tsx` |
| 95 | Stretchy Portfolio header | Pulling Portfolio down gently scales the balance, refreshing on release. | Design | 2 | 4 | 3 | 24 | `app/portfolio.tsx`, `src/ui/useRefreshControl.tsx` |
| 96 | Exports signed by your wallet | Your wallet signs the export's digest, so a recipient knows who produced it. | Sponsor | 2 | 4 | 3 | 24 | `app/export.tsx`, `src/export/deliver.ts` |
| 97 | Privy identity tokens, verified locally | Verify Privy identity tokens locally so linked wallets are known without API calls. | Sponsor | 2 | 4 | 3 | 24 | `src/auth/token.web.ts`, `src/auth/token.native.ts`, `server/src/auth/privy.ts`, `server/src/auth/middleware.ts` |
| 98 | Crash reports without a vendor | Screen crashes post route and build to the executor, listed on System. | Production | 2 | 4 | 3 | 24 | `src/errors/ErrorBoundary.tsx`, `server/src/routes/ops.ts`, `app/system.tsx` |
| 99 | Scheduler load test at scale | Prove one tick handles 500 strategies on a local fork, and publish the numbers. | Production | 2 | 4 | 3 | 24 | `server/src/executor/scheduler.ts`, `tools/load-scheduler.ts` (new) |
| 100 | Deposit from a linked wallet | Link an external wallet through Privy and move test USDC in on Sepolia builds. | Sponsor | 2 | 3 | 3 | 18 | `app/deposit.tsx`, `src/auth/useAuth.web.ts` |

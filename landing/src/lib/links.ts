/**
 * The hosted app. Every call to action on the page goes here — there is no waitlist to join.
 *
 * The X Layer build. `app.xorr.finance` is the Base build, a different product on a different chain; a page that
 * says "Built on X Layer" and sends its reader there would be the first wrong thing they saw.
 */
export const APP_URL = "https://xorr-xlayer.vercel.app";

export const GITHUB_URL = "https://github.com/nickthelegend/xorr-xlayer";
